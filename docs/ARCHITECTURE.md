# Architecture

<!-- Write this once the first real technical choice is made, and keep it current. The value is
     not the diagram; it is the record of what was rejected and why. Without that, every
     decision gets relitigated by the next person who has not seen the alternatives. -->

Decided 2026-09-21 by <actor> within the delegation in [`RECORD.md`](../RECORD.md). Each
decision names the alternatives that were considered, so a later reader can tell what was
rejected and why.

## The shape

<!-- One diagram, or one short paragraph. How the pieces fit together at run time, not the file
     tree. If a reader cannot tell where a request goes, the shape is not described yet. -->

TODO

## Decisions

<!-- One numbered decision per real choice. For each: what was chosen, the decisive reason, and
     the alternatives that were rejected with the reason. A decision without a rejected
     alternative is usually not a decision, it is a description. -->

### D1. TODO

TODO — what was chosen.

Chosen because TODO.

Rejected: **TODO** (reason), **TODO** (reason).

## What is deliberately absent

<!-- The fences. Things a reader might reasonably expect to find and will not, with the reason.
     This section prevents more rework than any other in the document. -->

TODO
